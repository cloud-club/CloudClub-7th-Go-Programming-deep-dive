syntax = "proto3";

option go_package = "grpc-chat/chatpb;chatpb";

package chat;

message ChatMessage {
  string user = 1;
  string message = 2;
  int64 timestamp = 3;
}

service ChatService {
  rpc ChatStream (stream ChatMessage) returns (stream ChatMessage);
}
